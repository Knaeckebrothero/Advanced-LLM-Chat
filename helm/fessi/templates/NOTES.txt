Thank you for installing {{ .Chart.Name }}!

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get all {{ .Release.Name }}

=============================================================================
ACCESSING THE APPLICATION
=============================================================================

{{- if .Values.frontend.ingress.enabled }}
Frontend is available at:
{{- range $host := .Values.frontend.ingress.hosts }}
  {{- range .paths }}
  http{{ if $.Values.frontend.ingress.tls }}s{{ end }}://{{ $host.host }}{{ .path }}
  {{- end }}
{{- end }}
{{- else }}
Frontend is available via port-forward:
  kubectl port-forward svc/{{ include "fessi.fullname" . }}-frontend 8080:{{ .Values.frontend.service.port }} -n {{ .Release.Namespace }}
  Then open: http://localhost:8080
{{- end }}

{{- if .Values.backend.ingress.enabled }}
Backend API is available at:
{{- range $host := .Values.backend.ingress.hosts }}
  {{- range .paths }}
  http{{ if $.Values.backend.ingress.tls }}s{{ end }}://{{ $host.host }}{{ .path }}
  {{- end }}
{{- end }}
{{- else }}
Backend API is available via port-forward:
  kubectl port-forward svc/{{ include "fessi.fullname" . }}-backend 8443:{{ .Values.backend.service.port }} -n {{ .Release.Namespace }}
  Then access: https://localhost:8443
{{- end }}

=============================================================================
DATABASE ACCESS (for debugging)
=============================================================================

PostgreSQL:
  kubectl port-forward svc/{{ include "fessi.fullname" . }}-postgresql 5432:{{ .Values.postgresql.service.port }} -n {{ .Release.Namespace }}

Neo4j Browser:
  kubectl port-forward svc/{{ include "fessi.fullname" . }}-neo4j 7474:{{ .Values.neo4j.service.httpPort }} -n {{ .Release.Namespace }}
  Then open: http://localhost:7474

=============================================================================
CONFIGURATION NOTES
=============================================================================

{{- if not .Values.secrets.replicateApiToken }}
WARNING: No Replicate API token configured!
  Set it with: --set secrets.replicateApiToken=YOUR_TOKEN
{{- end }}

{{- if not .Values.frontend.ingress.enabled }}
TIP: Enable ingress for production deployments:
  --set frontend.ingress.enabled=true
  --set frontend.ingress.hosts[0].host=your-domain.com
{{- end }}

For more configuration options, see the values.yaml file or run:
  helm show values ./helm/fessi
